"use client";
import { useForm } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import { z } from "zod";
import { contactSchema } from "@/lib/validations"; import { Button } from "@/components/ui/Button"; import { Input } from "@/components/ui/Input"; import { Textarea } from "@/components/ui/Textarea"; import { Select } from "@/components/ui/Select";
type T = z.infer<typeof contactSchema>;
export function ContactForm(){const {register,handleSubmit,formState:{errors,isSubmitting,isSubmitSuccessful}}=useForm<T>({resolver:zodResolver(contactSchema)}); const onSubmit=async()=>{await new Promise(r=>setTimeout(r,700));};
return <form onSubmit={handleSubmit(onSubmit)} className="space-y-3 panel rounded-xl p-6"><Input placeholder="Nome" {...register("name")}/>{errors.name&&<p className="text-red-400 text-xs">Nome obrigatório</p>}<Input placeholder="E-mail" {...register("email")}/><Input placeholder="WhatsApp" {...register("whatsapp")}/><Input placeholder="Empresa" {...register("company")}/><Select {...register("service")}><option value="">Tipo de serviço</option><option>Site institucional</option><option>Landing page</option><option>Sistema web</option></Select><Input placeholder="Orçamento" {...register("budget")}/><Input placeholder="Prazo" {...register("timeline")}/><Textarea placeholder="Mensagem" rows={4} {...register("message")}/><label className="text-xs text-muted flex gap-2"><input type="checkbox" {...register("consent")}/>Autorizo contato conforme LGPD.</label><Button disabled={isSubmitting}>{isSubmitting?"Enviando...":"Enviar mensagem"}</Button>{isSubmitSuccessful&&<p className="text-cyan text-xs">Mensagem validada e pronta para integração server-side.</p>}</form>}
